- content_for :header do
  = stylesheet_link_tag 'devise'
%h1
  %i.icon-cloud.icon.pull-right
  = link_to "Registrieren", root_path
= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
  .form-inputs
  = f.error_notification
  = f.input :email, :required => true, :placeholder =>"E-Mail Adresse", :autofocus => true, :label => false
  = f.input :password, :required => true, :label => false, :placeholder =>"Passwort"
  = f.input :password_confirmation, :required => true, :label => false, :placeholder =>"Passwort"
  = f.button :submit, "Registrieren", :class=> "btn btn-large btn-inverse"
  = link_to "Zurück", root_path, :class=> "btn btn-large btn-inverse"
%h2
  %i.icon-music.icon.pull-left
  %i.icon-group.icon.pull-right